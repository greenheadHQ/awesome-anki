# 이 파일은 참조용입니다. 실제 값은 .envrc에서 관리됩니다.
# - 시크릿: agenix 암호화 (secrets/*.age)
# - 로컬 설정: .envrc에 직접 export
#
# 시크릿 추가/수정: cd secrets && agenix -e <name>.age

# ===== 시크릿 (secrets/*.age) =====
GEMINI_API_KEY=your_gemini_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here
ANKI_SPLITTER_API_KEY=<generate with: openssl rand -hex 32>

# ===== 로컬 설정 (.envrc에 직접 export) =====
ANKI_CONNECT_URL=http://100.79.80.95:8765
ANKI_CONNECT_VERSION=6
TARGET_DECK=[책] 이것이 취업을 위한 컴퓨터 과학이다
ANKI_SPLITTER_PRIVACY_MASK_SENSITIVE=true
ANKI_SPLITTER_PRIVACY_MODE=balanced
ANKI_SPLITTER_PRIVACY_EMBEDDING_MAX_CHARS=2000
ANKI_SPLITTER_PRIVACY_SPLIT_MAX_CHARS=6000
ANKI_SPLITTER_PRIVACY_VALIDATION_MAX_CHARS=4000

# ===== 웹 개발 DX 도구 (선택) =====
VITE_LOCATOR_TARGET=cursor
VITE_DISABLE_LOCATOR=false
VITE_DISABLE_REACT_SCAN=false
VITE_DISABLE_REACT_GRAB=false
